<template>
  <div class="home-banner">
    <XtxCarousel :sliders="sliders" :autoPlay="3000"/>
  </div>
</template>
<script>
import { ref } from 'vue'
import { getBanner } from '@/api/home'
export default {
  name: 'HomeBanner',
  setup () {
    // 获取轮播图数据
    const sliders = ref([])
    getBanner().then(data => {
      sliders.value = data.result
    })
    return { sliders }
  }
}
</script>
<style scoped lang="less">
.home-banner {
  width: 1240px;
  height: 500px;
  position: absolute;
  left: 0;
  top: 0;
  z-index: 98
}
.xtx-carousel {
   /deep/.carousel-btn.prev {
    left: 270px;
  }
   /deep/.carousel-indicator {
    padding-left: 250px;
  }
}
</style>
